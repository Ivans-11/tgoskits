# 共同写作规范（每位 section 作者必须完整遵守）

你在为一份正式的中文技术报告（LaTeX，ctexart 文档类）撰写**一个小节**。报告主题：
一台网球拾取机器人，其操作系统由 C/Linux 迁移到以 Rust 实现的 Starry，运行于 RK3588
（瑞芯微，OrangePi 5 Plus 开发板）。这份报告把整个系统当作一个全栈系统来呈现，因此
每个主要部件都有自己的小节，**先把架构讲清楚（面向完全不懂的读者），再讲我们做了什么，
再把遇到的技术难点展开到寄存器 / ABI / 代码层级，最后给出验证与影响**。

## 输出要求

- 把你的 LaTeX 写到我在每个任务里指定的绝对路径文件。
- 文件里**只包含一个 `\subsection{...}`**（除非任务明确要求 `\section`），带上我给你的
  `\label{...}`，以及小节正文。**不要**输出 `\documentclass`、导言区、`\section`（除非被要求）、
  `\begin{document}`。这个文件会被 `\input` 进主文档。
- 不要写"占位""TODO""待补"之类。写不出来的事实就省略，绝不编造。

## 写作风格（硬性，用户特别强调）

- 正式、专业的技术中文。语气与下方"范例小节"保持一致。
- **彻底本地化**。不要音译，不要生造或别扭的术语（例如绝不要"硅片"这种词）。用自然的中文技术
  说法；英文缩写在首次出现时可用一个括号简短解释（例如"IOMMU（设备侧地址翻译部件）"）。
- **全文不得出现破折号**（不要 `—`，不要 `--`，不要中文破折号"——"）。
- **尽量不用冒号**。用流畅的句子，不要用冒号去引出列表式的罗列。
- **绝不预告结构**。不要写"本节介绍……""下面讨论……""首先……其次……""综上"这类话。
  展示，而不是声明。
- 段落为主，列表克制。需要时可用 `\begin{itemize} \item ... \end{itemize}`，但不要把整节写成列表。

## 可用的宏与环境（导言区已定义，直接用）

- `\texttt{...}`：代码、路径、寄存器名、设备节点、十六进制值。
- `\href{https://github.com/rcore-os/tgoskits/pull/NNNN}{\#NNNN}`：引用合并请求。
- 表格用 `tabularx`，列类型 `R`（右对齐弹性列）、`C`（居中弹性列）、`X`（左对齐弹性列）；
  表头单元格用 `\hdr{标题}`，表头行前加 `\headrow`；外加 `booktabs` 的 `\toprule \midrule \bottomrule`。
- 代码片段（仅在确有帮助时，且要短）：
  ```
  \begin{lstlisting}[caption={说明文字},captionpos=b]
  // 注释用中文
  ...
  \end{lstlisting}
  ```
- 插图（PNG 由用户稍后生成，现在用占位也能编译）：用我提供的这个命令，传入我给你的
  图片文件名、图注、标签：
  ```
  \repfig{文件名.png}{图注文字}{fig:标签}
  ```
  不要自己写 `\begin{figure}` 或 `\includegraphics`，统一用 `\repfig`。每节最多一张图，
  且仅当图确实有助于解释架构时才放。

## 诚实性（硬性）

- 每一个数字、合并请求编号、寄存器值、地址、标识符，都必须来自你**亲自读过的一手来源**，
  或来自我在任务里给你的"基准事实"。**不得编造任何**数字、尺寸、地址、型号或标识符。
  你的来源或基准事实里没有的细节，就不要写。
- 与 Linux 的推理对比，最高只能说"持平 / 处于同一水平"，**绝不能说"超过 / 更快 / 胜过"**。
  Starry 在某些中位数上更低，是因为它把加速器时钟固定在最高频而不动态降频，这是调频策略
  差异，不是硬件或推理更快。
- 凡是"绕过 / 回退 / 变通"的手段，要如实说明它是变通而非根因修复（例如块设备的轮询回退）。

## 范例小节（这就是目标语气与深度。请照这个深度写你的部件）

```latex
\subsection{RGA 二维加速}\label{sec:rga}

机器人采集到的每一帧并不能直接送入检测网络。相机交付的是 YUV 排布的像素，而 YOLOv8 的
输入要求是连续的 RGB 平面，尺寸还需缩放到模型的输入分辨率。若交由 CPU 逐像素完成色彩空间
转换与缩放，一帧的开销与一次推理相当，感知与计算彼此争抢同一个核，流水线随即被这一步拖住。
RK3588 为此集成了 RGA 二维栅格图形加速器，它在硬件中完成色彩空间转换、缩放与图层拼接，
并能直接读写物理内存，使这一步从计算路径上让出。

RGA 的软件入口在 Linux 上是 \texttt{/dev/rga} 与用户态的 librga。应用以一份 \texttt{rga\_req}
描述源与目标的地址、格式、尺寸与裁剪窗口，经一次 \texttt{RGA\_BLIT\_SYNC} 同步阻塞调用提交，
内核把它翻译为一段命令块，交由 RGA2 引擎执行。本队在 Starry 上重建了这条通路，一个平台无关
的 \texttt{\#![no\_std]} 驱动核心负责命令块编码与寄存器时序，\texttt{/dev/rga} 设备节点对齐
librga 的 ioctl 约定，使既有的 librga 二进制无需改动即可驱动这块硬件。

\paragraph{对齐用户态的二进制接口}
librga 与内核之间并非函数调用，而是一份按字节对齐的结构体经 ioctl 传递，任何一处字段错位都会
让引擎读到错误的地址或尺寸。\texttt{rga\_req} 在 LP64 下恰为 504 字节，其中嵌入三份各 56 字节
的图像描述符，缓冲导入用的 \texttt{rga\_external\_buffer} 为 288 字节，这些尺寸以编译期断言固定
下来，任何字段增删都会在编译时暴露。早期的镜像漏掉了 \texttt{rotate\_mode}、\texttt{rd\_mode}
等几个较新的字段，使其后所有字段整体偏移，引擎据此取到错位的旋转矩阵与地址。librga 在打开设备
时还会查询驱动版本与硬件版本以选择匹配的结构体布局，因此设备节点须如实报告 MultiRGA v1.3.1
与 RK3588 上 RGA2 核的版本号，librga 才会按这一布局组包并把请求投向 RGA2 而非另一代核心。

\paragraph{在三个核中找到正确的那一个}
RK3588 的设备树把 RGA 暴露为三个独立设备，两个 RGA3 核与一个 RGA2 核。本流水线所需的色彩转换
与缩放走 RGA2 路径，若按设备枚举顺序取首个核，落到的是一个没有 RGA2 引擎的 RGA3 核，提交随即
以无此设备失败。设备节点因此按版本而非按枚举序检索，锁定真正承载 RGA2 的那个核再提交。

\paragraph{让引擎真正跑起来并报告完成}
RGA2 的执行时序对寄存器写入的顺序敏感。引擎须先被置入主模式，命令起始信号才会触发一次命令
DMA 取指，停留在从模式时起始信号什么也取不到。完成信号同样有讲究，表示全部命令完成的状态位
只有在对应的中断使能于起始之前被置位时才会锁存。最初省略了这一步，引擎明明已经跑完，完成位
却始终为零，轮询一路超时，看上去像是硬件挂死。据厂商时序在主模式与起始之间补上这次对中断
使能位的读改写后，完成位正常锁存，既有的忙等轮询即可观察到它，无需真正布线中断。

\paragraph{一个把缩放挂死的定点系数}
缩放系数以 16.16 定点表示。缩小时正确的系数是目标维度比源维度，其值不超过一，写入低半字；
最初误用了它的倒数，缩小时系数大于一，使源指针每步跨过有效区域，扫描器随即停在忙状态约五十
毫秒不再前进。把缩小与放大两种情形分别按厂商参数计算后，缩放回到一次提交即完成。

\paragraph{让三个加速器读同一段内存}
RGA 在本流水线里不孤立工作，它读上一级解码的输出，又把结果直接写入 NPU 的输入缓冲。这要求
三者共享同一段物理内存而非各自拷贝。缓冲经 \texttt{/dev/dma\_heap} 在 4\,GiB 以下分配，以
dma-buf 文件描述符在各设备间传递，\texttt{/dev/rga} 收到描述符后解析出物理地址交给引擎，并在
提交与轮询期间持有该分配的引用计数，使另一线程的并发释放不会在操作中途抽走内存。RGA2 在本
阶段以 MMU 关闭、物理直通的方式运行，因此提交前后须显式完成缓存与设备方向的同步，引擎与 CPU
才看到一致的数据。

驱动的五项基本操作在开发板上以逐像素 CRC 校验通过自测，该工作以合并请求
\href{https://github.com/rcore-os/tgoskits/pull/1388}{\#1388} 提交至上游。接入这条路径后，
缩放一步由 15.82\,ms 降至 1.72\,ms，约九倍；色彩空间转换的 p50 为 0.776\,ms。这一步把图像
准备从计算路径上彻底让出，为后续把推理放置到大核腾出了余量。
```

## 你的工作方式

1. 先读你被指派的一手来源文件（驱动代码、应用代码、合并请求页等），核实细节。
2. 再按"架构先行 → 我们做了什么 → 技术难点（展开到寄存器/ABI/代码）→ 验证与影响"组织。
3. 数字与编号只用一手来源或我给的基准事实。拿不准就省略。
4. 写到指定文件。完成后用三五句话回报：你写了什么、引用了哪些一手来源、有没有任何你
   无法核实而选择省略的点。
